# Development, test and build dependencies.
#
# Regenerate the lock after editing this file:
#
#   uv pip compile --universal --generate-hashes --python-version 3.11 \
#       requirements-dev.in -o requirements-dev.txt

-r requirements.in

# Test suite.
pytest>=9.0.3
pytest-qt>=4.5.0

# Windows binary packaging (lol-viewer.spec).
pyinstaller>=6.22.3

# HTML parsing for scripts/fetch_champions.py (CI tooling, not shipped).
beautifulsoup4>=4.15.0

# NOTE: pip-audit lives in requirements-ci.in, not here -- the audit job must
# not have to install PyQt6 just to run a scanner.
